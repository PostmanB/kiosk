services:
  kiosk:
    build: .
    environment:
      VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
      VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
      VITE_KIOSK_PIN: ${VITE_KIOSK_PIN}
      VITE_STATS_PIN: ${VITE_STATS_PIN}
    ports:
      - "9999:80"
